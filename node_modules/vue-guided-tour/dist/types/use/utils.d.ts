import { BoundingRect, Position } from '../types';
export declare const getBoundingWithPadding: (rect: BoundingRect, padding?: number) => {
    top: number;
    right: number;
    bottom: number;
    left: number;
    width: number;
    height: number;
};
export declare const isInView: ({ top, left, bottom, right, }: Omit<BoundingRect, 'width' | 'height'>) => boolean;
export declare const isOutView: ({ top, left, bottom, right, }: Omit<BoundingRect, 'width' | 'height'>) => {
    top: boolean;
    left: boolean;
    right: boolean;
    bottom: boolean;
};
export declare const isPositionVertical: (position: Position) => boolean;
export declare const rafThrottle: <F extends (...args: any[]) => void>(fn: F) => (...args: Parameters<F>) => void;
export declare const getWindowCenterRect: () => {
    top: number;
    left: number;
    right: number;
    bottom: number;
    width: number;
    height: number;
};
